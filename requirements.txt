# Python deps for the Vercel serverless function (api/index.py → backend FastAPI).
# Deliberately NOT `-r backend/requirements.txt`: serverless doesn't need uvicorn
# (Vercel serves the ASGI app directly), and uvicorn[standard]'s native deps
# (uvloop/httptools) are a common build failure on Vercel's Python.
# backend/requirements.txt remains the source of truth for local/Docker runs.
fastapi==0.115.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pydantic[email]==2.9.2
pyjwt==2.9.0
cryptography==43.0.1
bcrypt==4.2.0
google-auth==2.35.0
requests==2.32.3
resend==2.4.0
