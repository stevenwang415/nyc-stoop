# zh-TW Localization — QA Report (Batch D, 2026-08-19)

## Overnight batch outcomes
- **Batch A (venues + figures wiring)** — done in-session 8/18: museum/venue pages (character, description, booking, admission, duration) and figure bios (tagline, nationality, primer) render in Chinese. Verified: Van Gogh page (荷蘭 · 把苦難畫成色彩的畫家 · full primer).
- **Batch B (works corpus)** — scheduled run at 00:38 produced nothing; re-run this morning: **184/184 works translated** (paintings, Broadway shows, buildings, albums — description, significance, whatToLookFor viewing notes) and wired via `tWorkF`. 7 entries' whatToLookFor arrays fall back to English (source-extraction artifacts; see Known issues).
- **Batch C (UI sweep)** — scheduled run at 02:32 wrapped the strings in App.jsx but never added dictionary entries; completed this morning: **94 keys added** (Events detail sheet, Eat hero, Map screen + tutorial fragments, import flow, PDF labels, plural templates).
- **Batch D (QA)** — this report.

## Checks
| Check | Result |
|---|---|
| `vite build` | ✅ passes |
| Dictionary coverage (every `t()`/`t2()` key in App.jsx + nycEvents.js) | ✅ 321/321 keys covered (ZH dict: 390 entries) |
| Simplified-character lint (i18n.js + both sidecars) | ✅ clean (信/息 hits are legit Traditional compounds 資訊/訊息) |
| Sidecar integrity (module import) | ✅ PLACES 524 · TOPICS 44 · DOMAINS 8 · GROUPS 7 · VENUES 103 · FIGURES 139 · WORKS 184 |
| Headless visual pass, all 5 tabs @390×844, `nyc_lang=zh` | ✅ screenshots taken; zero UI leaks. English strings on Explore/Events are live event titles + venue names (proper nouns — correct). |

## Known issues / follow-ups
1. **7 works show English "What to look for"**: kline_chief, picasso_demoiselles, braque_violin_palette, leger_smoker, hopper_sunday_morning, hopper_gas + 1 jazz entry — the extraction regex split items on escaped apostrophes, so their zh arrays were dropped (safe English fallback). Fix: re-extract those 7 with a proper parser and retranslate (10-minute job).
2. **PDF export in Chinese untested** — jsPDF CJK risk. If the exported PDF shows blank boxes, bundle a NotoSansTC subset font and register it with jsPDF before rendering.
3. Works `title` fields intentionally stay English everywhere (nav titles, cards).
4. Spanish (Batch E) not started — mirrors this exact pipeline when ready.

## On-device checklist (Steven, ~10 min)
1. `npm run dev` → Settings → Language → **中文** — whole app re-renders.
2. Incognito window → onboarding 3 slides in Chinese → 開始探索！
3. Explore → 瀏覽 → 視覺藝術 → 後印象派 → tap 梵谷 (bio in Chinese) → tap 《星夜》 (觀看重點 numbered notes in Chinese).
4. Tap MoMA venue page — 中文 character paragraph, 門票/所需時間 tiles, 訂位資訊, 內行人提示.
5. Events tab → detail sheet (購票/路線/加入行事曆 buttons).
6. Map tab → toggle 地圖/鄰里, tap a pin.
7. 幫我規劃！ flow: 時間/每人預算/地點 → 建立我的行程 →.
8. Save a plan → My Plans → reopen → per-day 路線 buttons.
9. **PDF download from a saved plan — check Chinese characters render** (issue #2 above).
10. Switch back to English — everything returns instantly.

Then for the real build: `npm run build && npx cap sync ios` and repeat 3–4 spot checks on the phone.
