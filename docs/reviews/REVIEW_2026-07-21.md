# REVIEW — 2026-07-21 (post-cancellation rebuild; anchor: 9.75 / 9.8)

## 1. Headline

The user pulled the submission on 07-16 because real-device testing surfaced
what the headless rig couldn't: an information architecture that reviewers
called "too complicated," meals that appeared and vanished on their own, and
saved plans that didn't match what was built. Five days later the app has a
different spine: **five tabs with one noun each** (Explore · Events · Map ·
Planner · My Plans), a Planner that shows **only what the user added** (meals
are opt-in via "🍴 Add a restaurant"; suggestions are pinned by id and never
change underneath you), events as **first-class schedule cards** that work
without dates, saved plans that replay the **exact card order** with events,
notes, and honest per-person pricing (admissions counted, "+ ticket" when a
show is in the day), a Food map layer that grew 16 → 64 pins, and a PDF
export that works in the native shell. Every fix in this cycle came from the
owner's own device testing, and every one was verified headless before
handing back. **Maya 9.75 → 9.8, Tom & Rachel 9.8 → 9.85. Verdict: GO after
the store metadata is refreshed** — the app outran its own screenshots.

## 2. Scores (prev → new)

| Dimension | Maya (local) | Tom & Rachel (visitors) |
|---|---|---|
| First impression | 9.25 → **9.3** | 9.6 → 9.6 |
| Clarity of purpose | 9.5 → **9.75** | 9.5 → **9.75** |
| Content relevance | 9.75 → 9.75 | 9.85 → 9.85 |
| Navigation & flow | 9.8 → **9.85** | 9.8 → **9.85** |
| Trust & credibility | 9.7 → **9.8** | 9.7 → **9.85** |
| Delight & return | 9.75 → 9.75 | 9.9 → 9.9 |
| **Overall** | **9.75 → 9.8** | **9.8 → 9.85** |

Justifications (anti-inflation: every move names shipped behavior):

- **Clarity (+0.25 both):** the single biggest strategic fix of the cycle.
  "My Trip" — one page doing workbench + archive + share — became **Planner**
  (auto-saving workbench) and **My Plans** (shelf), each a nav tab, with one
  verb family ("+ Add to Planner" → "Save copy" → My Plans). And the planner
  stopped volunteering content: no auto-suggested restaurants (opt-in
  button), no phantom meals riding along with stops. The reviewer complaint
  that triggered the cancellation ("which is the main point of my app?") has
  a structural answer now, not a wording patch.
- **Navigation (+0.05 both):** the last known dead end died — "+ Add to
  Planner" now *always* produces a visible card, even for a place previously
  ✕'d out of the plan (explicit-add-wins in toggleSave). Post-save, a
  "Start fresh" banner resets to a true first-open page (verified down to
  the sample-weekend button working, which the reset used to silently break).
  Removing the last card auto-clears the leftover day frames.
- **Trust (Maya +0.1, T&R +0.15):** every count on every surface now derives
  from the *rendered cards* (header "2 stops" = the 2 cards below it; "N
  meals picked" inventory noise deleted); suggestions are pinned so removing
  a stop can never silently swap a restaurant (verified: Caravaggio +
  J.G. Melon identical across two removals); saved plans are WYSIWYG (exact
  order replay incl. events, ✎ notes, map chips); price estimates stopped
  lying by omission — museum admissions count ("Adults $25 · Students $15" →
  $25), free is $0, unknown stays out, events append "+ ticket". Visitors
  gain more because the saved plan and its PDF are *their* artifact on the
  sidewalk.
- **First impression (Maya +0.05):** the empty Planner is one coherent state
  (map emoji, one sentence, one button) regardless of how you arrived —
  first install, Start fresh, or removing everything.
- **Content, Delight (flat):** dataset unchanged this cycle; the Food layer
  64-pin fix is exposure of existing content, not new content — it shows up
  in Navigation/Trust, not Content.

## 3. What shipped since 07-16 (the cancellation cycle)

**IA:** 5-tab nav (Planner 🔧 / My Plans 📁); all "My Trip" strings renamed;
My Plans empty state ("Start Building"); Save copy lands on My Plans with the
fresh card flash-highlighted; Share lives on the saved plan only.
**Events:** first-class cards (number badge, drag/nudge, note field for
ticket prices, ADDED EVENT pill); collect-then-pin (saving never touches
dates); undated plans pin events to Day 1; an event alone *creates* Day 1 —
the first-session "save a show → see a schedule" path works with zero setup.
**Meals:** opt-in per day ("🍴 Add a restaurant"; Plan-my-night + sample
weekend seed it — their promise is "routed, with food"); picks pinned by id,
unpinned only by ↻ Change / cuisine / reset; ≤20-min commute cap using the
same estimator as the printed connectors; event-venue anchoring; own
restaurants silence the matching slot but the button now fills the *other*
meal (two meals a day); Budget 💸 cuisine chip (the $ tier); lunch and dinner
both survive stop removals; restaurant stop cards dropped "⋯ Options" (one
adjustment surface: the suggestion card); swap sheet price-ascending + nav
clearance; restaurant swaps pull food venues, not itinerary-guessed museums.
**Saved plans:** itemOrder exact replay; events + notes render; counts from
rendered cards; map chip = the nav's mapPin SVG on all three card types,
vertically centered; Reserve is a body link; time ranges removed from
summaries; "+ ticket(s)" pricing.
**Map:** 64 food pins (curated restaurants with in-repo coords — the
no-Google-coords-on-Leaflet rule holds); tap card with Reserve / Directions /
Add to Planner, seed-deduped.
**Platform:** PDF export falls back to a hidden-iframe print in the Capacitor
WKWebView (the "allow pop-ups" dead end is gone); drag auto-scroll at screen
edges; number badges on every card incl. Google-sourced adds, with map pins
carrying matching explicit numbers.

## 4. Hands-on verification (this pass, current build)

15 headless suites, ~90 assertions, sequentially against tonight's dist:
saved-order replay 9/9 · Start-fresh lifecycle 17/18 · opt-in meals 6/6 ·
event-only night 7/7 · event notes 4/4 · 20-min radius + ticket pricing 9/9 ·
own-restaurant suppression 4/5 · meal-survives-removal 5/5 · empty auto-reset
5/5 · pinned picks 6/6 · Budget + Options removal 5/5 · two-meals 5/5 · map
food layer 4/4 · explicit-add-wins 4/4. The five nominal FAILs were all
stale test expectations from behavior the owner deliberately changed
(pre-opt-in meals ×3, renamed button label, intentionally removed swap path)
— **zero regressions found**. Zero page errors across all suites.

## 5. What's working — keep this

The invitation-first grammar. Every surprise the app used to spring —
auto-meals, anchor-hopping suggestions, events quarantined on a shelf, dead
add-buttons — is now either the user's explicit act or one labeled tap away.
That single principle ("only what I added, everything one tap away") resolved
more reviewer confusion than any copy change of the previous three cycles.

## 6. Issues

**Must-fix (before resubmission — all operational, none in-app):**
1. *Store metadata says "MY TRIP".* apply_store.md's description block and
   review notes describe a tab that no longer exists. Hurts: reviewer trust
   (the listed feature map won't match the build). Fix: re-run the listing
   copy against the 5-tab reality before pasting into ASC.
2. *Screenshots are stale a fourth time.* appstore-01..05 predate the 5-tab
   nav, the Planner rename, and the new card grammar. Same fix as always:
   retake at freeze, and freeze is again NOW.
3. *Device pass for the print sheet.* The iframe-print fallback is verified
   headless; `window.print()` in WKWebView needs one confirmation tap on the
   owner's phone (iOS 16+ expected fine).

**Should-fix (v1.1 candidates):**
4. *User-restaurant swap has no path.* With "⋯ Options" removed from
   restaurant stops, swapping means ✕ + re-add. Acceptable, but watch beta
   feedback. (Owner's explicit call, 07-21.)
5. *Budget chip vs. band label.* The chip promises "$20 and below"; picks
   show "≈$15–25/person" (the $ tier). Truthful but slightly off-message —
   consider "≈$25 and under" phrasing or a dedicated ≤$20 filter.
6. *Undated multi-day trips put every event on Day 1.* Correct for the
   tonight flow; a 3-day undated trip with three shows will want spreading.

**Polish:**
7. Cuisine grid is 9 chips in a 4-column grid (one orphan row).
8. `nyc_meal_picks` grows unboundedly across plans until a reset clears it.

## 7. Scorecard

| | Maya | Tom & Rachel |
|---|---|---|
| **Overall** | **9.8** | **9.85** |

## 8. The one thing to fix first

**Refresh the store package** — listing copy + screenshots + review notes
(issues 1–2). Nothing in the app blocks resubmission; the only thing that
can now earn a rejection is the *gap between the store's story and the
build*. Half a day: freeze, retake five screenshots, rewrite the MY TRIP
paragraph as PLANNER/MY PLANS, update the demo-account notes, submit.

## Addendum (same day) — discovery-side scenario walk

The morning pass leaned on plan-building journeys; this walk covered the
OTHER half of the rubric (§3 scenarios 2, 3, 4, 6, 7) — the surfaces most
exposed to this cycle's toggleSave rewiring. Hands-on, fresh profile,
LES geolocation mocked:

- **S7 First open:** onboarding renders → lands on home cleanly.
- **S6 Find one place:** search "Katz" → full detail page (area, admission,
  time budget) → "+ Add to Planner" saves on first tap. The explicit-add
  rewiring holds on the detail-page path.
- **S4 Where should we eat:** Eat browser loads with All-NYC/borough/Near-me
  scoping, Filters drawer (cuisine · price · neighborhood) opens, a row
  (Don Angie) opens its sheet with price band + facts, save works, and —
  the payoff of this cycle — the restaurant lands in the Planner as THE
  card: no uninvited suggestion beside it, with "🍴 Add a restaurant"
  offered for the second meal.
- **S2 Near me:** geolocation resolves and re-sorts (nearest-first).
- **S3 Events:** renders live Ticketmaster content in the harness (Tonight /
  This weekend / category chips, ★ Stoop picks, real listings). Not a
  cached fixture — the backend responded.
- **Map:** opens on the Neighborhoods schematic with the 9-domain legend;
  the area-sheet tap couldn't be automated (schematic hit-areas aren't text
  nodes — harness limitation, not a defect; the live-map pin → card → add
  flow passed in the morning battery).

**Scores unchanged** — the walk confirmed rather than contradicted the
morning's numbers. One new finding:

- **Polish (new):** venue detail pages label restaurant pricing as
  "Admission" ("Admission · $$$ — $90–130/person" on Don Angie). Dinner
  isn't admission — the label should be "Price" for food venues. One-line
  conditional; hurts the visitor's trust-in-polish a hair, nobody's journey.

## 9. Bottom line

7.1 → … → 9.7/9.75 → 9.75/9.8 → **9.8 / 9.85**. The canceled submission was
the right call: what device testing exposed wasn't bugs so much as a wrong
default philosophy (the app volunteering content users didn't ask for), and
this cycle replaced it wholesale with invitation-first behavior — verified,
regression-free. Update the store's story to match the app, run the phone
pass once more, and ship it.
