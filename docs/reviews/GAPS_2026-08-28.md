# NYC Stoop — open gaps (2026-08-28)

Delta since REVIEW_REDESIGN_2026-08-26. CLOSED since then: all six review
items (device pass ✓, interests wired into Browse-by/Events/official feed ✓,
deep-screen sweep + glance ✓, map polish + search ring ✓, remote access ✓),
official account seeded with the curated 14 + prod seed script, tab restore
after iOS eviction, LAN-safe copy button, runtime-refs check tooling.

What remains, by weight:

## 🚨 Blockers for a good v2.0 launch

1. **Stale App Store screenshots.** Every current screenshot shows the OLD
   design — after the reskin they misrepresent the app. New set needed
   (Explore, Map with sheet, Trips timeline, You/Stoop, Events) in EN +
   zh-Hant. This quietly became the largest un-started task.
2. **Native-build verification (Xcode, one session):** camera location
   prompt in Share compose (the one never-device-tested path),
   `npm install && npx cap sync ios`, general native pass of the redesign
   (safe areas, fonts, tab restore inside the webview).
3. **CDN dependency in the native app:** Leaflet, markercluster, and rotate
   load from unpkg at runtime — the Map tab needs network even though the
   guide is offline-first. Bundle them (npm deps + import) before
   submission. Old rider, sharpened by the map rebuild.

## ⚠️ Should close before / with the push

4. **The four parked v2.0 additions** (Steven's hold): interests editor in
   Settings (without it, every EXISTING user is locked out of the whole
   personalization layer forever) · "What's new in 2.0" card (Trips merge +
   You tab need one screen of orientation) · "Invite a friend" share-sheet
   button (cheapest growth loop) · native rating prompt. First two are
   near-mandatory; all four ≈ one sitting.
5. **PrivacyInfo.xcprivacy** (Apple privacy manifest) — required-reasons API
   declarations; old rider, now submission-relevant.
6. **Friends beta feedback** — tunnel works, beta hasn't run yet. Its fix
   list deserves room before submission.
7. **Interest-ranking blind spot:** the six coordinate-pinned official
   photos (Guggenheim, bookshops, Aladdin, Hi Collar, Fifty Lan, Little
   Italy) never get interest-boosted. Fix: add them as dataset places
   (guide-worthy anyway) or rank by kind as fallback.

## 🔧 Small / opportunistic

8. zh on-device pass incl. CJK PDF export (old rider, one walk in 中文).
9. "Admission" → "Price" copy rider; promo codes; gate Google sign-in
   button on native (old riders).
10. Bricolage small-size + map daylight tint — Steven's eyeballs, pending.

## ⏳ External waits (not ours)

IAP / W-8BEN (Apple) · Queens coverage (2.1) · public-visibility decision ·
trip albums, push, QR, dark mode (2.1 by design).

## Suggested order

Friends beta runs NOW (needs nothing) → parked four + interests blind spot
(one sitting) → Xcode session (native checks + Leaflet bundling + privacy
manifest) → screenshots + store copy → PREFLIGHT push sequence.
