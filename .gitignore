# ── dependencies ──
node_modules
.pnp
.pnp.js

# ── build output ──
dist
dist-ssr
*.local

# ── vite scratch files ──
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ── environment ──
.env
.env.local
SECRETS.local.md
.env.development.local
.env.test.local
.env.production.local

# ── Python (backend/) ──
.venv
venv
__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.mypy_cache
.ruff_cache
*.egg-info

# ── deploy ──
.vercel

# ── editor / OS ──
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.swp
Thumbs.db

# ── logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Capacitor (iOS folder is generated; commit only config) ──
ios/App/Pods
ios/App/build
ios/App/Podfile.lock
ios/DerivedData
android/.gradle
android/.idea
android/build
android/app/build
android/local.properties

# Session scratch files (underscore-prefixed) — safe to delete locally
_*
scripts/_*
.env*
