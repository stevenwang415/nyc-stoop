# ── dependencies ──
node_modules
.pnp
.pnp.js

# ── build output ──
dist
dist-ssr
*.local

# ── vite scratch files ──
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# ── environment ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── deploy ──
.vercel

# ── editor / OS ──
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.swp
Thumbs.db

# ── logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Capacitor (iOS folder is generated; commit only config) ──
ios/App/Pods
ios/App/build
ios/App/Podfile.lock
ios/DerivedData
android/.gradle
android/.idea
android/build
android/app/build
android/local.properties
